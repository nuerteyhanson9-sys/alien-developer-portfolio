# Contact form backend (JSON POST endpoint). Replace before deploy.
VITE_BACKEND_URL=https://your-backend.onrender.com

# GitHub Pages subpath. Set to "/" if hosting on a custom domain (alien.dev).
VITE_BASE_PATH=/alien-developer-portfolio/